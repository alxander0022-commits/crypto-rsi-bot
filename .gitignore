# local logs
bot.log
news.log
news_log.txt

# trading server — secrets & local db (NEVER commit)
server/.env
server/tradebot.db

# python
__pycache__/
*.pyc
.venv/
venv/

# NOTE: state.json and ledger.csv are intentionally NOT ignored.
# The GitHub Action commits them back so your position + paper record
# persist across hourly runs. If you run only locally, you can add them
# here to keep them out of git.
